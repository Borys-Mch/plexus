import React from 'react';
import styles from '../style';

export const Footer = () => {
  return (
    <section id='contacts' className={`${styles.sectionBox} lg:h-[220px] md:h-[340px] h-[600px] bg-[#1E1421]`}>
      <div className={`${styles.sectionSecond}`}></div>
    </section>
  )
}
